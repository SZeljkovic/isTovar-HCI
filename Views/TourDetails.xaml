<Window x:Class="Projekat_B_isTovar.Views.TourDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Projekat_B_isTovar.Resources"
        Title="{Binding Title}" Height="500" Width="700"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource BackgroundColor}">

    <Window.Resources>
        <local:TourDetailsStrings x:Key="Strings"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0">
            <StackPanel>
                <TextBlock Text="{Binding SourceCompanies}" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryColor}" Margin="0,0,0,10"/>
                <StackPanel x:Name="SourceCompaniesContainer" Margin="0,0,0,20"/>

                <TextBlock Text="{Binding DestinationCompanies}" FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryColor}" Margin="0,0,0,10"/>
                <StackPanel x:Name="DestinationCompaniesContainer"/>
            </StackPanel>
        </ScrollViewer>

        <Button Content="{Binding BackButton}" Grid.Row="1" Width="120" Height="35"
                HorizontalAlignment="Right" Margin="0,20,0,0"
                Background="{DynamicResource PrimaryColor}"
                Click="BackButton_Click"/>
    </Grid>
</Window>
